<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HymnApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HymnApp" parent="Base.Theme.HymnApp" />

    <string name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </string>

    <style name="MyRatingBar" parent="Widget.AppCompat.RatingBar">
        <!--Non Selected star color-->
        <item name="colorControlNormal">#4D000000</item>

        <!--Selected star color-->
        <item name="colorControlActivated">@color/dialog_color_1</item>
    </style>
</resources>